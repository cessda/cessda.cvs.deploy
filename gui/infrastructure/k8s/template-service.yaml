kind: Service
apiVersion: v1
metadata:
  name: SERVICENAME
  namespace: NAMESPACE
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: frontend-webui
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: SERVICENAME
