kind: Service
apiVersion: v1
metadata:
  name: SERVICENAME
  namespace: NAMESPACE
spec:
  type: ClusterIP
  ports:
  - name: frontend-api
    port: 8080
    targetPort: frontend-cport
    protocol: TCP
  selector:
    app: SERVICENAME
